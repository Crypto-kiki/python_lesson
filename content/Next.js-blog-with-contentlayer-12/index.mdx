---
title: "Next.js blog with contentlayer: 12. SEO & Image Optimization"
description: "SEO (Search Engine Optimization) and image optimization for a Next.js blog using Contentlayer."
image: "../../public/blogs/nextjs-blog-contentlayer/12/mainCover.png"
publishedAt: "2024-11-01"
updatedAt: "2024-11-01"
author: "Baekki"
isPublished: true
tags:
  - web development
---

## Previous post

<a href="https://baekki-dev-blog.vercel.app/blogs/Next.js-blog-with-contentlayer-11">
  11. Blog views using Supabase
</a>

이어서 진행합니다.

## SEO(Search Engine Optimization)

SEO(검색 엔진 최적화)는 웹사이트나 웹 페이지를 검색 엔진에서 더 잘 보이게 하기 위해 콘텐츠, 링크 구성 등을 최적화하는 과정입니다. SEO의 목적은 특정 키워드나 문구에 대한 검색 결과에서 웹사이트의 순위를 높여 더 많은 방문자와 잠재 고객을 유치 할 수 있습니다.

Contentlayer는 콘텐츠를 구조화하고 관리하기 쉬우며, 이는 SEO에 중요한 콘텐츠 최적화를 용이하게 합니다.

<a
  href="https://nextjs.org/docs/app/building-your-application/optimizing/metadata"
  target="_blank"
>
  Metadata
</a>

layout.js 파일을 보면 metadata가 기본으로 작성되어 있습니다.

```javascript title="src/app/layout.js"
export const metadata = {
  title: "Create Next App",
  description: "Generated by create next app",
};
```

<button
  onClick={() => {
    navigator.clipboard.writeText(`
export const metadata = {
  title: "Create Next App",
  description: "Generated by create next app",
};`);
    document.getElementById("copy1").textContent = "✔️ copied";
    setTimeout(() => {
      document.getElementById("copy1").textContent = "";
    }, 2000);
  }}
>
  <div>📄 Code Copy</div>
</button>
<span id="copy1" style={{ marginLeft: "10px", color: "green" }}></span>

블로그의 중요한 내용을 메타데이터에 작성해야 합니다. 먼저, utils폴더에 siteMetadata.js를 생성합니다.

```javascript title="src/uitls/siteMeatadata.js"
const siteMetadata = {
  title: "Next.js Blog With Tailwind CSS and Contentlayer",
  author: "baekki",
  headerTitle: "Next.js Blog",
  description: "A blog created with Next.js, Tailwind.css and contentlayer.",
  language: "ko-kr",
  theme: "system",
  siteUrl: "https://testSiteUrl.com",
  siteLogo: "/logo.png",
  socialBanner: "/social-banner.png",
  email: "baekki@gmail.com",
  github: "https://github.com/baekki",
  twitter: "https://twitter.com/baekki",
  linkedin: "https://www.linkedin.com/in/baekki/",
  locale: "en-US",
};

module.exports = siteMetadata;
```

<button
  onClick={() => {
    navigator.clipboard.writeText(`
const siteMetadata = {
  title: "Next.js Blog With Tailwind CSS and Contentlayer",
  author: "baekki",
  headerTitle: "Next.js Blog",
  description: "A blog created with Next.js, Tailwind.css and contentlayer.",
  language: "ko-kr",
  theme: "system",
  siteUrl: "https://testSiteUrl.com",
  siteLogo: "/logo.png",
  socialBanner: "/social-banner.png",
  email: "baekki@gmail.com",
  github: "https://github.com/baekki",
  twitter: "https://twitter.com/baekki",
  linkedin: "https://www.linkedin.com/in/baekki/",
  locale: "en-US",
};
module.exports = siteMetadata;`);
    document.getElementById("copy2").textContent = "✔️ copied";
    setTimeout(() => {
      document.getElementById("copy2").textContent = "";
    }, 2000);
  }}
>
  <div>📄 Code Copy</div>
</button>
<span id="copy2" style={{ marginLeft: "10px", color: "green" }}></span>

⚠️ siteUrl, github 등 링크는 배포 후 링크/개인 링크로 수정하시면 됩니다.

```javascript title="src/app/layout.js"
import { Inter, Manrope } from "next/font/google";
import "./globals.css";
import { cx } from "@/src/utils";
import Header from "@/src/components/Header";
import Footer from "../components/Footer";
import siteMetadata from "../utils/siteMetadata";

const inter = Inter({
  subsets: ["latin"],
  display: "swap",
  variable: "--font-in",
});
const manrope = Manrope({
  subsets: ["latin"],
  display: "swap",
  variable: "--font-mr",
});

export const metadata = {
  metadataBase: new URL(siteMetadata.siteUrl),
  title: {
    template: `%s | ${siteMetadata.title}`,
    default: siteMetadata.title,
  },
  description: siteMetadata.description,
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body
        className={cx(inter.variable, manrope.variable, "font-mr bg-light")}
      >
        <Header />
        {children}
        <Footer />
      </body>
    </html>
  );
}
```

<button
  onClick={() => {
    navigator.clipboard.writeText(`
import { Inter, Manrope } from "next/font/google";
import "./globals.css";
import { cx } from "@/src/utils";
import Header from "@/src/components/Header";
import Footer from "../components/Footer";
import siteMetadata from "../utils/siteMetadata";
const inter = Inter({
  subsets: ["latin"],
  display: "swap",
  variable: "--font-in",
});
const manrope = Manrope({
  subsets: ["latin"],
  display: "swap",
  variable: "--font-mr",
});

export const metadata = {
  metadataBase: new URL(siteMetadata.siteUrl),
  title: {
    template: \`%s | \${siteMetadata.title}\`,
    default: siteMetadata.title,
  },
  description: siteMetadata.description,
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body
        className={cx(inter.variable, manrope.variable, "font-mr bg-light")}
      >
        <Header />
        {children}
        <Footer />
      </body>
    </html>
  );
}`);
    document.getElementById("copy3").textContent = "✔️ copied";
    setTimeout(() => {
      document.getElementById("copy3").textContent = "";
    }, 2000);
  }}
>
  <div>📄 Code Copy</div>
</button>
<span id="copy3" style={{ marginLeft: "10px", color: "green" }}></span>

위 처럼 layout.js에 적용하면 title이 수정된 것을 확인 할 수 있습니다.

<Image
  src="/blogs/nextjs-blog-contentlayer/12/seo1.png"
  width="718"
  height="404"
  alt="Image"
  sizes="100vw"
/>

다음은 openGraph를 적용해봅시다.

<a
  href="https://nextjs.org/docs/app/api-reference/functions/generate-metadata#opengraph"
  target="_blank"
>
  Next js openGraph
</a>

```javascript title="src/app/layout.js"
import { Inter, Manrope } from "next/font/google";
import "./globals.css";
import { cx } from "@/src/utils";
import Header from "@/src/components/Header";
import Footer from "../components/Footer";
import siteMetadata from "../utils/siteMetadata";

const inter = Inter({
  subsets: ["latin"],
  display: "swap",
  variable: "--font-in",
});
const manrope = Manrope({
  subsets: ["latin"],
  display: "swap",
  variable: "--font-mr",
});

export const metadata = {
  metadataBase: new URL(siteMetadata.siteUrl),
  title: {
    template: `%s | ${siteMetadata.title}`,
    default: siteMetadata.title,
  },
  description: siteMetadata.description,
  openGraph: {
    title: siteMetadata.title,
    description: siteMetadata.description,
    url: siteMetadata.siteUrl,
    siteName: siteMetadata.title,
    images: [siteMetadata.socialBanner],
    locale: "ko-kr",
    type: "website",
  },
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body
        className={cx(inter.variable, manrope.variable, "font-mr bg-light")}
      >
        <Header />
        {children}
        <Footer />
      </body>
    </html>
  );
}
```

<button
  onClick={() => {
    navigator.clipboard.writeText(`
import { Inter, Manrope } from "next/font/google";
import "./globals.css";
import { cx } from "@/src/utils";
import Header from "@/src/components/Header";
import Footer from "../components/Footer";
import siteMetadata from "../utils/siteMetadata";
const inter = Inter({
  subsets: ["latin"],
  display: "swap",
  variable: "--font-in",
});
const manrope = Manrope({
  subsets: ["latin"],
  display: "swap",
  variable: "--font-mr",
});

export const metadata = {
  metadataBase: new URL(siteMetadata.siteUrl),
  title: {
    template: \`%s | \${siteMetadata.title}\`,
    default: siteMetadata.title,
  },
  description: siteMetadata.description,
  openGraph: {
    title: siteMetadata.title,
    description: siteMetadata.description,
    url: siteMetadata.siteUrl,
    siteName: siteMetadata.title,
    images: [siteMetadata.socialBanner],
    locale: "ko-kr",
    type: "website",
  },
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body
        className={cx(inter.variable, manrope.variable, "font-mr bg-light")}
      >
        <Header />
        {children}
        <Footer />
      </body>
    </html>
  );
}`);
    document.getElementById("copy4").textContent = "✔️ copied";
    setTimeout(() => {
      document.getElementById("copy4").textContent = "";
    }, 2000);
  }}
>
  <div>📄 Code Copy</div>
</button>
<span id="copy4" style={{ marginLeft: "10px", color: "green" }}></span>

다음으로 robots를 적용합니다.

```javascript title="src/app/layout.js"
import { Inter, Manrope } from "next/font/google";
import "./globals.css";
import { cx } from "@/src/utils";
import Header from "@/src/components/Header";
import Footer from "../components/Footer";
import siteMetadata from "../utils/siteMetadata";

const inter = Inter({
  subsets: ["latin"],
  display: "swap",
  variable: "--font-in",
});
const manrope = Manrope({
  subsets: ["latin"],
  display: "swap",
  variable: "--font-mr",
});

export const metadata = {
  metadataBase: new URL(siteMetadata.siteUrl),
  title: {
    template: `%s | ${siteMetadata.title}`,
    default: siteMetadata.title,
  },
  description: siteMetadata.description,
  openGraph: {
    title: siteMetadata.title,
    description: siteMetadata.description,
    url: siteMetadata.siteUrl,
    siteName: siteMetadata.title,
    images: [siteMetadata.socialBanner],
    locale: "ko-kr",
    type: "website",
  },
  robots: {
    index: true,
    follow: true,
    googleBot: {
      index: true,
      follow: false,
      noimageindex: true,
      "max-video-preview": -1,
      "max-image-preview": "large",
      "max-snippet": -1,
    },
  },
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body
        className={cx(inter.variable, manrope.variable, "font-mr bg-light")}
      >
        <Header />
        {children}
        <Footer />
      </body>
    </html>
  );
}
```

<button
  onClick={() => {
    navigator.clipboard.writeText(`
import { Inter, Manrope } from "next/font/google";
import "./globals.css";
import { cx } from "@/src/utils";
import Header from "@/src/components/Header";
import Footer from "../components/Footer";
import siteMetadata from "../utils/siteMetadata";
const inter = Inter({
  subsets: ["latin"],
  display: "swap",
  variable: "--font-in",
});
const manrope = Manrope({
  subsets: ["latin"],
  display: "swap",
  variable: "--font-mr",
});

export const metadata = {
  metadataBase: new URL(siteMetadata.siteUrl),
  title: {
    template: \`%s | \${siteMetadata.title}\`,
    default: siteMetadata.title,
  },
  description: siteMetadata.description,
  openGraph: {
    title: siteMetadata.title,
    description: siteMetadata.description,
    url: siteMetadata.siteUrl,
    siteName: siteMetadata.title,
    images: [siteMetadata.socialBanner],
    locale: "ko-kr",
    type: "website",
  },
  robots: {
    index: true,
    follow: true,
    googleBot: {
      index: true,
      follow: false,
      noimageindex: true,
      "max-video-preview": -1,
      "max-image-preview": "large",
      "max-snippet": -1,
    },
  },
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body
        className={cx(inter.variable, manrope.variable, "font-mr bg-light")}
      >
        <Header />
        {children}
        <Footer />
      </body>
    </html>
  );
}`);
    document.getElementById("copy5").textContent = "✔️ copied";
    setTimeout(() => {
      document.getElementById("copy5").textContent = "";
    }, 2000);
  }}
>
  <div>📄 Code Copy</div>
</button>
<span id="copy5" style={{ marginLeft: "10px", color: "green" }}></span>

- metadata 객체 내의 robots는 검색 엔진 로봇(크롤러)이 웹 페이지를 어떻게 처리해야 하는지 지시하는 데 사용됩니다.
- index : 검색 엔진이 이 페이지를 인덱스에 추가할 수 있는지 여부를 나타냅니다. true로 설정되면, 검색 엔진은 이 페이지를 인덱싱하여 검색 결과에 표시할 수 있습니다.
- follow : 이 속성은 페이지 내의 링크를 검색 엔진이 따를 수 있는지 여부를 지정합니다. true로 설정되면, 검색 엔진은 페이지에 있는 모든 링크를 따라갈 수 있습니다.
- googleBot: 이 객체는 구글 검색 엔진 로봇인 Googlebot에 대한 추가적인 지침을 제공합니다.
- max-video-preview : 검색 결과에서 비디오 미리보기의 최대 길이를 지정합니다. -1로 설정하면 미리보기가 허용되지 않습니다.
- max-image-preview: 검색 결과에서 이미지 미리보기의 최대 크기를 지정합니다. 'large'로 설정하면 큰 이미지 미리보기가 허용됩니다.
- max-snippet: 검색 결과에서 표시될 수 있는 페이지의 텍스트 스니펫의 최대 길이를 지정합니다. -1로 설정하면 스니펫이 표시되지 않습니다.

다음으로 twitter 를 작성해봅시다.

<a
  href="https://nextjs.org/docs/app/api-reference/functions/generate-metadata#twitter"
  target="_blank"
>
  meatadata-twitter
</a>

```javascript title="src/app/layout.js"
import { Inter, Manrope } from "next/font/google";
import "./globals.css";
import { cx } from "@/src/utils";
import Header from "@/src/components/Header";
import Footer from "../components/Footer";
import siteMetadata from "../utils/siteMetadata";

const inter = Inter({
  subsets: ["latin"],
  display: "swap",
  variable: "--font-in",
});
const manrope = Manrope({
  subsets: ["latin"],
  display: "swap",
  variable: "--font-mr",
});

export const metadata = {
  metadataBase: new URL(siteMetadata.siteUrl),
  title: {
    template: `%s | ${siteMetadata.title}`,
    default: siteMetadata.title,
  },
  description: siteMetadata.description,
  openGraph: {
    title: siteMetadata.title,
    description: siteMetadata.description,
    url: siteMetadata.siteUrl,
    siteName: siteMetadata.title,
    images: [siteMetadata.socialBanner],
    locale: "ko-kr",
    type: "website",
  },
  robots: {
    index: true,
    follow: true,
    googleBot: {
      index: true,
      follow: false,
      noimageindex: true,
      "max-video-preview": -1,
      "max-image-preview": "large",
      "max-snippet": -1,
    },
  },
  twitter: {
    card: "summary_large_image",
    title: siteMetadata.title,
    images: [siteMetadata.socialBanner],
  },
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body
        className={cx(inter.variable, manrope.variable, "font-mr bg-light")}
      >
        <Header />
        {children}
        <Footer />
      </body>
    </html>
  );
}
```

<button
  onClick={() => {
    navigator.clipboard.writeText(`
import { Inter, Manrope } from "next/font/google";
import "./globals.css";
import { cx } from "@/src/utils";
import Header from "@/src/components/Header";
import Footer from "../components/Footer";
import siteMetadata from "../utils/siteMetadata";
const inter = Inter({
  subsets: ["latin"],
  display: "swap",
  variable: "--font-in",
});
const manrope = Manrope({
  subsets: ["latin"],
  display: "swap",
  variable: "--font-mr",
});

export const metadata = {
  metadataBase: new URL(siteMetadata.siteUrl),
  title: {
    template: \`%s | \${siteMetadata.title}\`,
    default: siteMetadata.title,
  },
  description: siteMetadata.description,
  openGraph: {
    title: siteMetadata.title,
    description: siteMetadata.description,
    url: siteMetadata.siteUrl,
    siteName: siteMetadata.title,
    images: [siteMetadata.socialBanner],
    locale: "ko-kr",
    type: "website",
  },
  robots: {
    index: true,
    follow: true,
    googleBot: {
      index: true,
      follow: false,
      noimageindex: true,
      "max-video-preview": -1,
      "max-image-preview": "large",
      "max-snippet": -1,
    },
  },
  twitter: {
    card: "summary_large_image",
    title: siteMetadata.title,
    images: [siteMetadata.socialBanner],
  },
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body
        className={cx(inter.variable, manrope.variable, "font-mr bg-light")}
      >
        <Header />
        {children}
        <Footer />
      </body>
    </html>
  );
}`);
    document.getElementById("copy6").textContent = "✔️ copied";
    setTimeout(() => {
      document.getElementById("copy6").textContent = "";
    }, 2000);
  }}
>
  <div>📄 Code Copy</div>
</button>
<span id="copy6" style={{ marginLeft: "10px", color: "green" }}></span>

파일을 저장 후 블로그에서 개발자도구로 `<head>`태그를 확인해봅시다.

<Image
  src="/blogs/nextjs-blog-contentlayer/12/seo2.png"
  width="718"
  height="404"
  alt="Image"
  sizes="100vw"
/>

메타데이터로 생성한 title, url, site_name 등 확인 할 수 있습니다.

이제 dynamic routes에 메타데이터를 적용하는 방법을 알아봅시다.

<a
  href="https://nextjs.org/docs/app/building-your-application/optimizing/metadata#dynamic-metadata"
  target="_blank"
>
  dynamic-metadata
</a>

```javascript title="src/app/blogs/[slug]/page.js"
import { allBlogs } from ".contentlayer/generated";
import BlogDetails from "@/src/components/Blog/BlogDetails";
import RenderMdx from "@/src/components/Blog/RenderMdx";
import Tag from "@/src/components/Elements/Tag";
import siteMetadata from "@/src/utils/siteMetadata";
import { slug } from "github-slugger";
import Image from "next/image";

export async function generateStaticParams() {
  return allBlogs.map((blog) => ({ slug: blog._raw.flattenedPath }));
}

export async function generateMetadata({ params }) {
  const blog = allBlogs.find((blog) => blog._raw.flattenedPath === params.slug);

  if (!blog) {
    return;
  }

  const publishedAt = new Date(blog.publishedAt).toISOString();
  const modifiedAt = new Date(blog.updatedAt || blog.publishedAt).toISOString();

  let imageList = [siteMetadata.socialBanner];
  if (blog.image) {
    imageList =
      typeof blog.image.filePath === "string"
        ? [siteMetadata.siteUrl + blog.image.filePath.replace("../public", "")]
        : blog.image;
  }

  const ogImages = imageList.map((img) => {
    return { url: img.includes("http") ? img : siteMetadata.siteUrl + img };
  });

  const authors = blog?.author ? [blog.author] : siteMetadata.author;

  return {
    title: blog.title,
    description: blog.description,
    openGraph: {
      title: blog.title,
      description: blog.description,
      url: siteMetadata.siteUrl + blog.url,
      siteName: siteMetadata.title,
      locale: "ko-kr",
      type: "article",
      publishedTime: publishedAt,
      modifiedTime: modifiedAt,
      images: ogImages,
      author: authors.length > 0 ? authors : [siteMetadata.author],
    },
  };
}

export default function BlogPage({ params }) {
  const blog = allBlogs.find((blog) => blog._raw.flattenedPath === params.slug);

  return (
    <article>
      <div className="mb-8 text-center relative w-full h-[70vh] bg-dark">
        <div className="w-full z-10 flex flex-col items-center justify-center absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2">
          <Tag
            name={blog.tags[0]}
            link={`/categories/${slug(blog.tags[0])}`}
            className="px-6 text-sm py-2"
          />
          <h1 className="inline-block mt-6 font-semibold capitalize text-light text-5xl leading-normal relative w-5/6">
            {blog.title}
          </h1>
        </div>
        <div className="absolute top-0 left-0 right-0 bottom-0 h-full bg-dark/60" />
        <Image
          src={blog.image.filePath.replace("../public", "")}
          placeholder="blur"
          blurDataURL={blog.image.blurhashDataUrl}
          alt={blog.title}
          width={blog.image.width}
          height={blog.image.height}
          className=" aspect-square w-full h-full object-cover object-center"
        />
      </div>
      <BlogDetails blog={blog} slug={params.slug} />
      <div className="grid grid-cols-12 gap-16 mt-8 px-10">
        <div className="col-span-4">
          <details
            className="border border-dark text-dark rounded-lg p-4 sticky top-6 max-h-[80vh] overflow-hidden overflow-y-auto"
            open
          >
            <summary className="text-lg font-semibold capitalize cursor-pointer">
              Table Of Contents
            </summary>
            <ul className="mt-4 font-in">
              {blog.toc.map((heading) => {
                return (
                  <li key={`#${heading.slug}`} className="py-1">
                    <a
                      href={`#${heading.slug}`}
                      data-level={heading.level}
                      className="data-[level=two]:pl-0 data-[level=two]:pt-2
                    data-[level=two]:border-b border-dark/40 data-[level=three]:pl-6
                    flex items-center"
                    >
                      {heading.level === "three" ? (
                        <span className="flex w-1 h-1 rounded-full bg-dark mr-2 ">
                          &nbsp;
                        </span>
                      ) : null}
                      <span className="hover:underline">{heading.text}</span>
                    </a>
                  </li>
                );
              })}
            </ul>
          </details>
        </div>
        <RenderMdx blog={blog} />
      </div>
    </article>
  );
}
```

<button
  onClick={() => {
    navigator.clipboard.writeText(`
import { allBlogs } from ".contentlayer/generated";
import BlogDetails from "@/src/components/Blog/BlogDetails";
import RenderMdx from "@/src/components/Blog/RenderMdx";
import Tag from "@/src/components/Elements/Tag";
import siteMetadata from "@/src/utils/siteMetadata";
import { slug } from "github-slugger";
import Image from "next/image";

export async function generateStaticParams() {
  return allBlogs.map((blog) => ({ slug: blog._raw.flattenedPath }));
}

export async function generateMetadata({ params }) {
  const blog = allBlogs.find((blog) => blog._raw.flattenedPath === params.slug);
  if (!blog) {
    return;
  }
  const publishedAt = new Date(blog.publishedAt).toISOString();
  const modifiedAt = new Date(blog.updatedAt || blog.publishedAt).toISOString();
  let imageList = [siteMetadata.socialBanner];
  if (blog.image) {
    imageList =
      typeof blog.image.filePath === "string"
        ? [siteMetadata.siteUrl + blog.image.filePath.replace("../public", "")]
        : blog.image;
  }
  const ogImages = imageList.map((img) => {
    return { url: img.includes("http") ? img : siteMetadata.siteUrl + img };
  });
  const authors = blog?.author ? [blog.author] : siteMetadata.author;
  return {
    title: blog.title,
    description: blog.description,
    openGraph: {
      title: blog.title,
      description: blog.description,
      url: siteMetadata.siteUrl + blog.url,
      siteName: siteMetadata.title,
      locale: "ko-kr",
      type: "article",
      publishedTime: publishedAt,
      modifiedTime: modifiedAt,
      images: ogImages,
      author: authors.length > 0 ? authors : [siteMetadata.author],
    },
  };
}

export default function BlogPage({ params }) {
  const blog = allBlogs.find((blog) => blog._raw.flattenedPath === params.slug);
  return (
    <article>
      <div className="mb-8 text-center relative w-full h-[70vh] bg-dark">
        <div className="w-full z-10 flex flex-col items-center justify-center absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2">
          <Tag
            name={blog.tags[0]}
            link={\`/categories/\${slug(blog.tags[0])}\`}
            className="px-6 text-sm py-2"
          />
          <h1 className="inline-block mt-6 font-semibold capitalize text-light text-5xl leading-normal relative w-5/6">
            {blog.title}
          </h1>
        </div>
        <div className="absolute top-0 left-0 right-0 bottom-0 h-full bg-dark/60" />
        <Image
          src={blog.image.filePath.replace("../public", "")}
          placeholder="blur"
          blurDataURL={blog.image.blurhashDataUrl}
          alt={blog.title}
          width={blog.image.width}
          height={blog.image.height}
          className=" aspect-square w-full h-full object-cover object-center"
        />
      </div>
      <BlogDetails blog={blog} slug={params.slug} />
      <div className="grid grid-cols-12 gap-16 mt-8 px-10">
        <div className="col-span-4">
          <details
            className="border border-dark text-dark rounded-lg p-4 sticky top-6 max-h-[80vh] overflow-hidden overflow-y-auto"
            open
          >
            <summary className="text-lg font-semibold capitalize cursor-pointer">
              Table Of Contents
            </summary>
            <ul className="mt-4 font-in">
              {blog.toc.map((heading) => {
                return (
                  <li key={\`#\${heading.slug}\`} className="py-1">
                    <a
                      href={\`#\${heading.slug}\`}
                      data-level={heading.level}
                      className="data-[level=two]:pl-0 data-[level=two]:pt-2
                    data-[level=two]:border-b border-dark/40 data-[level=three]:pl-6
                    flex items-center"
                    >
                      {heading.level === "three" ? (
                        <span className="flex w-1 h-1 rounded-full bg-dark mr-2 ">
                          &nbsp;
                        </span>
                      ) : null}
                      <span className="hover:underline">{heading.text}</span>
                    </a>
                  </li>
                );
              })}
            </ul>
          </details>
        </div>
        <RenderMdx blog={blog} />
      </div>
    </article>
  );
}`);
    document.getElementById("copy7").textContent = "✔️ copied";
    setTimeout(() => {
      document.getElementById("copy7").textContent = "";
    }, 2000);
  }}
>
  <div>📄 Code Copy</div>
</button>
<span id="copy7" style={{ marginLeft: "10px", color: "green" }}></span>

위 generateMetadata함수를 사용해서 각 포스팅마다 메타데이터를 동적으로 적용 할 수 있습니다.

<Image
  src="/blogs/nextjs-blog-contentlayer/12/seo3.png"
  width="718"
  height="404"
  alt="Image"
  sizes="100vw"
/>

위의 이미지처럼 각 포스팅마다 metadata도 적용된 것을 볼 수 있습니다.

category 페이지도 적용합니다.

```javascript title="src/app/categories/[slug]/page.js"
import { allBlogs } from "@/.contentlayer/generated";
import BlogLayoutThree from "@/src/components/Blog/BlogLayoutThree";
import Categories from "@/src/components/Blog/Categories";
import GithubSlugger, { slug } from "github-slugger";

const slugger = new GithubSlugger();

export async function generateStaticParams() {
  const categories = [];
  const paths = [{ slug: "all" }];

  allBlogs.map((blog) => {
    if (blog.isPublished) {
      blog.tags.map((tag) => {
        const slugified = slugger.slug(tag);
        if (!categories.includes(slugified)) {
          categories.push(slugified);
          paths.push({ slug: slugified });
        }
      });
    }
  });

  return paths;
}

export async function generateMetadata({ params }) {
  return {
    title: `${params.slug.replaceAll("-", " ")} Blogs`,
    description: `Learn more about ${
      params.slug === "all" ? "seb development" : params.slug
    }} `,
  };

const CategoryPage = ({ params }) => {
  const allCategories = ["all"];
  const blogs = allBlogs.filter((blog) => {
    return blog.tags.some((tag) => {
      const slugified = slug(tag);

      if (!allCategories.includes(slugified)) {
        allCategories.push(slugified);
      }

      if (params.slug === "all") {
        return true;
      }

      return slugified === params.slug;
    });
  });

  return (
    <article className="mt-12 flex flex-col text-dark ">
      <div className="px-32 flex flex-col ">
        <h1 className="mt-6 font-semibold text-5xl">#{params.slug}</h1>
        <span className="mt-2 inline-block ">
          Discover more categories and expand your knowledge!
        </span>
      </div>
      <Categories categories={allCategories} currentSlug={params.slug} />

      <div className="grid grid-cols-3 grid-rows-2 gap-16 mt-24 px-32">
        {blogs.map((blog, index) => (
          <article key={index} className="col-span-1 row-span-1 relative">
            <BlogLayoutThree blog={blog} />
          </article>
        ))}
      </div>
    </article>
  );
};

export default CategoryPage;
```

<button
  onClick={() => {
    navigator.clipboard.writeText(`
import { allBlogs } from "@/.contentlayer/generated";
import BlogLayoutThree from "@/src/components/Blog/BlogLayoutThree";
import Categories from "@/src/components/Blog/Categories";
import GithubSlugger, { slug } from "github-slugger";

const slugger = new GithubSlugger();

export async function generateStaticParams() {
  const categories = [];
  const paths = [{ slug: "all" }];
  allBlogs.map((blog) => {
    if (blog.isPublished) {
      blog.tags.map((tag) => {
        const slugified = slugger.slug(tag);
        if (!categories.includes(slugified)) {
          categories.push(slugified);
          paths.push({ slug: slugified });
        }
      });
    }
  });
  return paths;
}
export async function generateMetadata({ params }) {
  return {
    title: \`$\{params.slug.replaceAll("-", " ")} Blogs\`,
    description: \`Learn more about \${
      params.slug === "all" ? "seb development" : params.slug
    }} \`,
  };
const CategoryPage = ({ params }) => {
  const allCategories = ["all"];
  const blogs = allBlogs.filter((blog) => {
    return blog.tags.some((tag) => {
      const slugified = slug(tag);
      if (!allCategories.includes(slugified)) {
        allCategories.push(slugified);
      }
      if (params.slug === "all") {
        return true;
      }
      return slugified === params.slug;
    });
  });
  return (
    <article className="mt-12 flex flex-col text-dark ">
      <div className="px-32 flex flex-col ">
        <h1 className="mt-6 font-semibold text-5xl">#{params.slug}</h1>
        <span className="mt-2 inline-block ">
          Discover more categories and expand your knowledge!
        </span>
      </div>
      <Categories categories={allCategories} currentSlug={params.slug} />
      <div className="grid grid-cols-3 grid-rows-2 gap-16 mt-24 px-32">
        {blogs.map((blog, index) => (
          <article key={index} className="col-span-1 row-span-1 relative">
            <BlogLayoutThree blog={blog} />
          </article>
        ))}
      </div>
    </article>
  );
};

export default CategoryPage;`);
    document.getElementById("copy8").textContent = "✔️ copied";
    setTimeout(() => {
      document.getElementById("copy8").textContent = "";
    }, 2000);
  }}
>
  <div>📄 Code Copy</div>
</button>
<span id="copy8" style={{ marginLeft: "10px", color: "green" }}></span>

마지막으로 header, footer의 소셜링크도 수정합니다.

```javascript title="src/components/Header/index.js"
import React from "react";
import Logo from "./logo";
import Link from "next/link";
import { GithubIcon, LinkedinIcon, SunIcon, TwitterIcon } from "../icons";
import siteMetadata from "@/src/utils/siteMetadata";

const Header = () => {
  return (
    <div className="w-full p-4 px-10 flex items-center justify-between">
      <Logo />
      <nav className="w-max py-3 px-8 border border-solid border-dark rounded-full font-medium capitalize flex items-center fixed top-6 right-1/2 translate-x-1/2 bg-light/80 backdrop-blur-sm z-50">
        <Link href="/" className="mr-2">
          Home
        </Link>
        <Link href="/about" className="mx-2">
          About
        </Link>
        <Link href="/contact" className="mx-2">
          Contact
        </Link>
        <button>
          <SunIcon />
        </button>
      </nav>
      <div>
        <a href={siteMetadata.linkedin} className="inline-block w-6 h-6 mr-4">
          <LinkedinIcon className="hover:scale-125 transition-all ease duration-200" />
        </a>
        <a href={siteMetadata.twitter} className="inline-block w-6 h-6 mr-4">
          <TwitterIcon className="hover:scale-125 transition-all ease duration-200" />
        </a>
        <a href={siteMetadata.github} className="inline-block w-6 h-6 mr-4">
          <GithubIcon className="hover:scale-125 transition-all ease duration-200" />
        </a>
      </div>
    </div>
  );
};

export default Header;
```

<button
  onClick={() => {
    navigator.clipboard.writeText(`
import React from "react";
import Logo from "./logo";
import Link from "next/link";
import { GithubIcon, LinkedinIcon, SunIcon, TwitterIcon } from "../icons";
import siteMetadata from "@/src/utils/siteMetadata";
const Header = () => {
  return (
    <div className="w-full p-4 px-10 flex items-center justify-between">
      <Logo />
      <nav className="w-max py-3 px-8 border border-solid border-dark rounded-full font-medium capitalize flex items-center fixed top-6 right-1/2 translate-x-1/2 bg-light/80 backdrop-blur-sm z-50">
        <Link href="/" className="mr-2">
          Home
        </Link>
        <Link href="/about" className="mx-2">
          About
        </Link>
        <Link href="/contact" className="mx-2">
          Contact
        </Link>
        <button>
          <SunIcon />
        </button>
      </nav>
      <div>
        <a href={siteMetadata.linkedin} className="inline-block w-6 h-6 mr-4">
          <LinkedinIcon className="hover:scale-125 transition-all ease duration-200" />
        </a>
        <a href={siteMetadata.twitter} className="inline-block w-6 h-6 mr-4">
          <TwitterIcon className="hover:scale-125 transition-all ease duration-200" />
        </a>
        <a href={siteMetadata.github} className="inline-block w-6 h-6 mr-4">
          <GithubIcon className="hover:scale-125 transition-all ease duration-200" />
        </a>
      </div>
    </div>
  );
};

export default Header;`);
    document.getElementById("copy9").textContent = "✔️ copied";
    setTimeout(() => {
      document.getElementById("copy9").textContent = "";
    }, 2000);
  }}
>
  <div>📄 Code Copy</div>
</button>
<span id="copy9" style={{ marginLeft: "10px", color: "green" }}></span>

```javascript title="src/components/Footer/index.js"
import React from "react";
import { GithubIcon, LinkedinIcon, TwitterIcon } from "../icons";
import Link from "next/link";
import siteMetadata from "@/src/utils/siteMetadata";

const Footer = () => {
  return (
    <footer className="mt-16 rounded-2xl bg-dark m-10 flex flex-col items-center text-light">
      <h3 className="mt-16 font-medium text-center capitalize text-4xl px-4">
        Explore the Future of Web & Blockchain
      </h3>
      <p className="mt-5 px-4 text-center w-3/4 font-light">
        Dive into the latest trends in front-end and blockchain technologies.
        <br />
        Subscribe for cutting-edge guides, insightful analyses, and engaging
        stories from the forefront of digital innovation.
      </p>
      <div className="flex items-center mt-8">
        <a href={siteMetadata.linkedin} className="inline-block w-6 h-6 mr-4">
          <LinkedinIcon className="hover:scale-125 transition-all ease duration-200" />
        </a>
        <a href={siteMetadata.twitter} className="inline-block w-6 h-6 mr-4">
          <TwitterIcon className="hover:scale-125 transition-all ease duration-200" />
        </a>
        <a href={siteMetadata.github} className="inline-block w-6 h-6 mr-4">
          <GithubIcon className="hover:scale-125 transition-all ease duration-200 fill-light" />
        </a>
      </div>
      <div className="w-full mt-24 relative font-medium border-t border-solid border-light py-6 px-8 flex items-center   justify-between">
        <span className="text-center">
          &copy; 2024 Baekki. All rights reserved.{" "}
        </span>
        <Link href="/sitemap.xml" className="text-center underline">
          sitemap.xml
        </Link>
        <div className="text-center">
          Made by{" "}
          <a href="http://localhost:3000/" className="underline">
            Baekki
          </a>
        </div>
      </div>
    </footer>
  );
};

export default Footer;
```

<button
  onClick={() => {
    navigator.clipboard.writeText(`
import React from "react";
import { GithubIcon, LinkedinIcon, TwitterIcon } from "../icons";
import Link from "next/link";
import siteMetadata from "@/src/utils/siteMetadata";
const Footer = () => {
  return (
    <footer className="mt-16 rounded-2xl bg-dark m-10 flex flex-col items-center text-light">
      <h3 className="mt-16 font-medium text-center capitalize text-4xl px-4">
        Explore the Future of Web & Blockchain
      </h3>
      <p className="mt-5 px-4 text-center w-3/4 font-light">
        Dive into the latest trends in front-end and blockchain technologies.
        <br />
        Subscribe for cutting-edge guides, insightful analyses, and engaging
        stories from the forefront of digital innovation.
      </p>
      <div className="flex items-center mt-8">
        <a href={siteMetadata.linkedin} className="inline-block w-6 h-6 mr-4">
          <LinkedinIcon className="hover:scale-125 transition-all ease duration-200" />
        </a>
        <a href={siteMetadata.twitter} className="inline-block w-6 h-6 mr-4">
          <TwitterIcon className="hover:scale-125 transition-all ease duration-200" />
        </a>
        <a href={siteMetadata.github} className="inline-block w-6 h-6 mr-4">
          <GithubIcon className="hover:scale-125 transition-all ease duration-200 fill-light" />
        </a>
      </div>
      <div className="w-full mt-24 relative font-medium border-t border-solid border-light py-6 px-8 flex items-center   justify-between">
        <span className="text-center">
          &copy; 2024 Baekki. All rights reserved.{" "}
        </span>
        <Link href="/sitemap.xml" className="text-center underline">
          sitemap.xml
        </Link>
        <div className="text-center">
          Made by{" "}
          <a href="http://localhost:3000/" className="underline">
            Baekki
          </a>
        </div>
      </div>
    </footer>
  );
};

export default Footer;`);
    document.getElementById("copy10").textContent = "✔️ copied";
    setTimeout(() => {
      document.getElementById("copy10").textContent = "";
    }, 2000);
  }}
>
  <div>📄 Code Copy</div>
</button>
<span id="copy10" style={{ marginLeft: "10px", color: "green" }}></span>

이제 sitemap을 추가합니다.

sitemap은 웹사이트의 페이지들을 나열한 목록입니다. 검색 엔진이 사이트를 더 효과적으로 크롤링하도록 도와주며 사이트의 구조를 보여주고, 검색 엔진이 웹사이트의 모든 페이지를 발견하고 인덱싱할 수 있도록 도와줍니다.

<a href="https://www.npmjs.com/package/next-sitemap" target="_blank">
  next-sitemap
</a>

> npm i next-sitemap

패키지 설치 후 package.json script에 추가합니다.

```json title="package.json"
{
  "name": "next-blog",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "postbuild": "next-sitemap",
    "start": "next start",
    "lint": "next lint"
  },
  "dependencies": {
    "@supabase/auth-helpers-nextjs": "^0.9.0",
    "contentlayer": "^0.3.4",
    "date-fns": "^3.6.0",
    "github-slugger": "^2.0.0",
    "next": "^13.5.6",
    "next-contentlayer": "^0.3.4",
    "next-sitemap": "^4.2.3",
    "react": "^18",
    "react-dom": "^18",
    "reading-time": "^1.5.0",
    "rehype-autolink-headings": "^7.1.0",
    "rehype-pretty-code": "^0.13.0",
    "rehype-slug": "^6.0.0",
    "remark-gfm": "^3.0.1",
    "shiki": "^1.2.1"
  },
  "devDependencies": {
    "@tailwindcss/typography": "^0.5.11",
    "autoprefixer": "^10.0.1",
    "eslint": "^8",
    "eslint-config-next": "14.1.3",
    "postcss": "^8",
    "tailwindcss": "^3.3.0"
  }
}
```

<button
  onClick={() => {
    navigator.clipboard.writeText(`
{
  "name": "next-blog",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "postbuild": "next-sitemap",
    "start": "next start",
    "lint": "next lint"
  },
  "dependencies": {
    "@supabase/auth-helpers-nextjs": "^0.9.0",
    "contentlayer": "^0.3.4",
    "date-fns": "^3.6.0",
    "github-slugger": "^2.0.0",
    "next": "^13.5.6",
    "next-contentlayer": "^0.3.4",
    "next-sitemap": "^4.2.3",
    "react": "^18",
    "react-dom": "^18",
    "reading-time": "^1.5.0",
    "rehype-autolink-headings": "^7.1.0",
    "rehype-pretty-code": "^0.13.0",
    "rehype-slug": "^6.0.0",
    "remark-gfm": "^3.0.1",
    "shiki": "^1.2.1"
  },
  "devDependencies": {
    "@tailwindcss/typography": "^0.5.11",
    "autoprefixer": "^10.0.1",
    "eslint": "^8",
    "eslint-config-next": "14.1.3",
    "postcss": "^8",
    "tailwindcss": "^3.3.0"
  }
}`);
    document.getElementById("copy11").textContent = "✔️ copied";
    setTimeout(() => {
      document.getElementById("copy11").textContent = "";
    }, 2000);
  }}
>
  <div>📄 Code Copy</div>
</button>
<span id="copy11" style={{ marginLeft: "10px", color: "green" }}></span>

root 디렉토리에 next-sitemap.config.js파일을 생성합니다.

```javascript title="next-sitemap.config.js"
const siteMetadata = require("./src/utils/siteMetadata");

module.exports = {
  siteUrl: siteMetadata.siteUrl,
  generateRobotsTxt: true,
};
```

<button
  onClick={() => {
    navigator.clipboard.writeText(`
const siteMetadata = require("./src/utils/siteMetadata");
module.exports = {
  siteUrl: siteMetadata.siteUrl,
  generateRobotsTxt: true,
};`);
    document.getElementById("copy12").textContent = "✔️ copied";
    setTimeout(() => {
      document.getElementById("copy12").textContent = "";
    }, 2000);
  }}
>
  <div>📄 Code Copy</div>
</button>
<span id="copy12" style={{ marginLeft: "10px", color: "green" }}></span>

파일 생성 후 build해봅시다.

> npm run build

build를 실행하면 몇 가지 오류가 발생해서 수정해보겠습니다.

1. ./src/app/layout.js<br/>
   65:9 Error: `next/script` components with inline content must specify an `id` attribute. See: https://nextjs.org/docs/messages/inline-script-id @next/next/inline-script-id

- `<Script>` 안에 인라인 스크립트가 사용되고 있지만, id 속성이 없어서 발생한 에러입니다. id를 추가해줍니다.

```javascript title="src/app/layout.js"
import { Inter, Manrope } from "next/font/google";
import "./globals.css";
import { cx } from "@/src/utils";
import Header from "@/src/components/Header";
import Footer from "../components/Footer";
import siteMetadata from "../utils/siteMetadata";
import Script from "next/script";

const inter = Inter({
  subsets: ["latin"],
  display: "swap",
  variable: "--font-in",
});
const manrope = Manrope({
  subsets: ["latin"],
  display: "swap",
  variable: "--font-mr",
});

export const metadata = {
  metadataBase: new URL(siteMetadata.siteUrl),
  title: {
    template: `%s | ${siteMetadata.title}`,
    default: siteMetadata.title,
  },
  description: siteMetadata.description,
  openGraph: {
    title: siteMetadata.title,
    description: siteMetadata.description,
    url: siteMetadata.siteUrl,
    siteName: siteMetadata.title,
    images: [siteMetadata.socialBanner],
    locale: "ko-kr",
    type: "website",
  },
  robots: {
    index: true,
    follow: true,
    googleBot: {
      index: true,
      follow: false,
      noimageindex: true,
      "max-video-preview": -1,
      "max-image-preview": "large",
      "max-snippet": -1,
    },
  },
  twitter: {
    card: "summary_large_image",
    title: siteMetadata.title,
    images: [siteMetadata.socialBanner],
  },
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body
        className={cx(
          inter.variable,
          manrope.variable,
          "font-mr bg-light dark:bg-dark"
        )}
      >
        <Script id="theme-script">
          {`if (localStorage.getItem('theme') === 'dark' || (!('theme' in localStorage) && window.matchMedia('(prefers-color-scheme: dark)').matches)) {
              document.documentElement.classList.add('dark')
            } else {
              document.documentElement.classList.remove('dark')
            }`}
        </Script>
        <Header />
        {children}
        <Footer />
      </body>
    </html>
  );
}
```

<button
  onClick={() => {
    navigator.clipboard.writeText(`
import { Inter, Manrope } from "next/font/google";
import "./globals.css";
import { cx } from "@/src/utils";
import Header from "@/src/components/Header";
import Footer from "../components/Footer";
import siteMetadata from "../utils/siteMetadata";
import Script from "next/script";
const inter = Inter({
  subsets: ["latin"],
  display: "swap",
  variable: "--font-in",
});
const manrope = Manrope({
  subsets: ["latin"],
  display: "swap",
  variable: "--font-mr",
});

export const metadata = {
  metadataBase: new URL(siteMetadata.siteUrl),
  title: {
    template: \`%s | \${siteMetadata.title}\`,
    default: siteMetadata.title,
  },
  description: siteMetadata.description,
  openGraph: {
    title: siteMetadata.title,
    description: siteMetadata.description,
    url: siteMetadata.siteUrl,
    siteName: siteMetadata.title,
    images: [siteMetadata.socialBanner],
    locale: "ko-kr",
    type: "website",
  },
  robots: {
    index: true,
    follow: true,
    googleBot: {
      index: true,
      follow: false,
      noimageindex: true,
      "max-video-preview": -1,
      "max-image-preview": "large",
      "max-snippet": -1,
    },
  },
  twitter: {
    card: "summary_large_image",
    title: siteMetadata.title,
    images: [siteMetadata.socialBanner],
  },
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body
        className={cx(
          inter.variable,
          manrope.variable,
          "font-mr bg-light dark:bg-dark"
        )}
      >
        <Script id="theme-script">
          {\`if (localStorage.getItem('theme') === 'dark' || (!('theme' in localStorage) && window.matchMedia('(prefers-color-scheme: dark)').matches)) {
              document.documentElement.classList.add('dark')
            } else {
              document.documentElement.classList.remove('dark')
            }\`}
        </Script>
        <Header />
        {children}
        <Footer />
      </body>
    </html>
  );
}`);
    document.getElementById("copy13").textContent = "✔️ copied";
    setTimeout(() => {
      document.getElementById("copy13").textContent = "";
    }, 2000);
  }}
>
  <div>📄 Code Copy</div>
</button>
<span id="copy13" style={{ marginLeft: "10px", color: "green" }}></span>

2. ./src/components/About/AboutCoverSection.js<br/>
   27:39 Error: `'` can be escaped with `&apos;`, `&lsquo;`, `&#39;`, `&rsquo;`. react/no-unescaped-entities <br/>
   27:69 Error: `'` can be escaped with `&apos;`, `&lsquo;`, `&#39;`, `&rsquo;`. react/no-unescaped-entities

- 이는 AboutcoverSection.js의 `<p>`태그 내에 you're, let's 내에서 아포스트로피(apostrophe)를 사용했기 때문에 발생한 문제 입니다. you&apos;re, let&apos;s 로 수정하거나 you are, let us로 수정하면 됩니다.

3. ./src/components/About/Skills.js <br/>
   19:10 Error: `'` can be escaped with `&apos;`, `&lsquo;`, `&#39;`, `&rsquo;`. react/no-unescaped-entities

- 위 에러도 2번과 동일합니다.

4. ./src/components/Home/RecentPosts.js <br/>
   27:13 Error: Missing "key" prop for element in iterator react/jsx-key

- 위 에러는 `<article>`태그에 key가 없어서 발생한 오류입니다. 배열의 각 요소를 반복해서 랜더링 할 때, 각 요소에 고유한 key prop이 누락되었기 때문입니다. key prop으로 index를 사용하면 됩니다.

빌드가 완료되면 아래와 같이 결과를 확인 할 수 있습니다.

<Image
  src="/blogs/nextjs-blog-contentlayer/12/seo4.png"
  width="718"
  height="404"
  alt="Image"
  sizes="100vw"
/>

빌드 후 재실행해보면 블로그 내에서 경로이동, 로딩속도 등 매우 빨라진 것을 확인 할 수 있습니다.

메인페이지 footer에 작성했던 sitemap.xml을 클릭해보세요.

<Image
  src="/blogs/nextjs-blog-contentlayer/12/seo5.png"
  width="718"
  height="404"
  alt="Image"
  sizes="100vw"
/>

⚠️ 배포 후 metadata에서 siteUrl을 배포한 주소로 수정해주세요.

`http://localhost:3000/sitemap-0.xml` 위 주소로 들어가보면 블로그의 모든 url을 확인 할 수 있습니다.

<Image
  src="/blogs/nextjs-blog-contentlayer/12/seo6.png"
  width="718"
  height="404"
  alt="Image"
  sizes="100vw"
/>

그런데 터미널에서 이미지 최적화 관련 경고 메세지를 확인 할 수 있습니다.

## Image Optimization

> Warning: For production Image Optimization with Next.js, the optional 'sharp' package is strongly recommended. Run 'npm i sharp', and Next.js will use it automatically for Image Optimization.

이미지 최적화를 해봅시다. Next.js에서 `<Image>`태그의 `sizes`속성을 조정하면, 이미지 로딩 시 성능과 최적화에 긍정적인 영향을 줄 수 있습니다.

<a
  href="https://nextjs.org/docs/pages/api-reference/components/image-legacy#sizes"
  target="_blank"
>
  nextjs-sizes
</a>

아래 코드블럭들을 확인하여 `<Image>`태그의 수정된 부분을 확인하세요.

```javascript title="src/components/Header/logo.js"
import Image from "next/image";
import Link from "next/link";
import profileImg from "@/public/profile.png";

const Logo = () => {
  return (
    <Link href="/" className="flex items-center text-dark dark:text-light">
      <div className="w-12 md:w-16 rounded-full overflow-hidden border border-solid border-dark dark:border-light mr-2 md:mr-4">
        <Image
          src={profileImg}
          alt="baekki"
          className="w-full h-auto rounded-r-full"
          sizes="33vw"
          priority
        />
      </div>
      <span className="font-bold dark:font-semibold text-lg md:text-xl">
        Baekki
      </span>
    </Link>
  );
};

export default Logo;
```

<button
  onClick={() => {
    navigator.clipboard.writeText(`
import Image from "next/image";
import Link from "next/link";
import profileImg from "@/public/profile.png";
const Logo = () => {
  return (
    <Link href="/" className="flex items-center text-dark dark:text-light">
      <div className="w-12 md:w-16 rounded-full overflow-hidden border border-solid border-dark dark:border-light mr-2 md:mr-4">
        <Image
          src={profileImg}
          alt="baekki"
          className="w-full h-auto rounded-r-full"
          sizes="33vw"
          priority
        />
      </div>
      <span className="font-bold dark:font-semibold text-lg md:text-xl">
        Baekki
      </span>
    </Link>
  );
};

export default Logo;`);
    document.getElementById("copy14").textContent = "✔️ copied";
    setTimeout(() => {
      document.getElementById("copy14").textContent = "";
    }, 2000);
  }}
>
  <div>📄 Code Copy</div>
</button>
<span id="copy14" style={{ marginLeft: "10px", color: "green" }}></span>

```javascript title="src/components/Home/HomeCoverSection.js"
import { sortBlogs } from "@/src/utils";
import Image from "next/image";
import Link from "next/link";
import React from "react";
import Tag from "../Elements/Tag";
import { slug } from "github-slugger";

const HomeCoverSection = ({ blogs }) => {
  const sortedBlogs = sortBlogs(blogs);
  const blog = sortedBlogs[0];

  return (
    <div className="w-full inline-block">
      <article className=" flex flex-col items-start justify-end mx-5 sm:mx-10 relative h-[60vh] sm:h-[85vh]">
        <div className="absolute top-0 left-0 bottom-0 right-0 h-full bg-gradient-to-b from-transparent from-0% to-dark/90 rounded-3xl z-0" />
        <Image
          src={blog.image.filePath.replace("../public", "")}
          placeholder="blur"
          blurDataURL={blog.image.blurhashDataUrl}
          alt={blog.title}
          fill
          className="w-full h-full object-center object-cover rounded-3xl -z-10"
          sizes="100vw"
          priority
        />

        <div className="w-full lg:w-3/4 p-6 sm:p-8 md:p-12 lg:p-16 flex flex-col items-start justify-center z-0 text-light">
          <Tag link={`/categories/${slug(blog.tags[0])}`} name={blog.tags[0]} />
          <Link href={blog.url} className="mt-6">
            <h1 className="font-bold capitalize text-lg sm:text-xl md:text-3xl lg:text-4xl">
              <span className="bg-gradient-to-r from-accent to-accent dark:from-accentDark/50 dark:to-accentDark/50 bg-[length:0px_0px] hover:bg-[length:100%_6px] bg-left-bottom bg-no-repeat transition-[background-size] duration-700">
                {blog.title}
              </span>
            </h1>
          </Link>
          <p className="hidden sm:inline-block mt-4 md:text-lg lg:text-xl font-in">
            {blog.description}
          </p>
        </div>
      </article>
    </div>
  );
};

export default HomeCoverSection;
```

<button
  onClick={() => {
    navigator.clipboard.writeText(`
import { sortBlogs } from "@/src/utils";
import Image from "next/image";
import Link from "next/link";
import React from "react";
import Tag from "../Elements/Tag";
import { slug } from "github-slugger";
const HomeCoverSection = ({ blogs }) => {
  const sortedBlogs = sortBlogs(blogs);
  const blog = sortedBlogs[0];
  return (
    <div className="w-full inline-block">
      <article className=" flex flex-col items-start justify-end mx-5 sm:mx-10 relative h-[60vh] sm:h-[85vh]">
        <div className="absolute top-0 left-0 bottom-0 right-0 h-full bg-gradient-to-b from-transparent from-0% to-dark/90 rounded-3xl z-0" />
        <Image
          src={blog.image.filePath.replace("../public", "")}
          placeholder="blur"
          blurDataURL={blog.image.blurhashDataUrl}
          alt={blog.title}
          fill
          className="w-full h-full object-center object-cover rounded-3xl -z-10"
          sizes="100vw"
          priority
        />
        <div className="w-full lg:w-3/4 p-6 sm:p-8 md:p-12 lg:p-16 flex flex-col items-start justify-center z-0 text-light">
          <Tag link={\`/categories/\${slug(blog.tags[0])}\`} name={blog.tags[0]} />
          <Link href={blog.url} className="mt-6">
            <h1 className="font-bold capitalize text-lg sm:text-xl md:text-3xl lg:text-4xl">
              <span className="bg-gradient-to-r from-accent to-accent dark:from-accentDark/50 dark:to-accentDark/50 bg-[length:0px_0px] hover:bg-[length:100%_6px] bg-left-bottom bg-no-repeat transition-[background-size] duration-700">
                {blog.title}
              </span>
            </h1>
          </Link>
          <p className="hidden sm:inline-block mt-4 md:text-lg lg:text-xl font-in">
            {blog.description}
          </p>
        </div>
      </article>
    </div>
  );
};

export default HomeCoverSection;`);
    document.getElementById("copy15").textContent = "✔️ copied";
    setTimeout(() => {
      document.getElementById("copy15").textContent = "";
    }, 2000);
  }}
>
  <div>📄 Code Copy</div>
</button>
<span id="copy15" style={{ marginLeft: "10px", color: "green" }}></span>

```javascript title="src/components/Blog/BlogLayoutOne.js"
import Image from "next/image";
import React from "react";
import Tag from "../Elements/Tag";
import Link from "next/link";

const BlogLayoutOne = ({ blog }) => {
  return (
    <div className="group inline-block overflow-hidden rounded-xl">
      <div className=" absolute top-0 left-0 bottom-0 right-0 h-full bg-gradient-to-b from-transparent from-0% to-dark/90 rounded-xl z-00" />
      <Image
        src={blog.image.filePath.replace("../public", "")}
        placeholder="blur"
        blurDataURL={blog.image.blurhashDataUrl}
        alt={blog.title}
        width={blog.image.width}
        height={blog.image.height}
        className="w-full h-full object-center object-cover rounded-xl group-hover:scale-105 transition-all ease duration-300"
        sizes="(max-width: 1180px) 100vw, 50vw"
      />
      <div className=" w-full absolute bottom-0 p-4 xs:p-6 sm:p-10 z-20">
        <Tag
          link={`/categories/${blog.tags[0]}`}
          name={blog.tags[0]}
          className="px-6 text-xs sm:text-sm py-1 sm:py-2 !border"
        />
        <Link href={blog.url} className="mt-6">
          <h2 className="font-bold capitalize text-sm xs:text-base sm:text-xl md:text-2xl text-light mt-2 sm:mt-4">
            <span className="bg-gradient-to-r from-accent to-accent bg-[length:0px_6px] group-hover:bg-[length:100%_6px] bg-left-bottom bg-no-repeat transition-[background-size] duration-500">
              {blog.title}
            </span>
          </h2>
        </Link>
      </div>
    </div>
  );
};

export default BlogLayoutOne;
```

<button
  onClick={() => {
    navigator.clipboard.writeText(`
import Image from "next/image";
import React from "react";
import Tag from "../Elements/Tag";
import Link from "next/link";
const BlogLayoutOne = ({ blog }) => {
  return (
    <div className="group inline-block overflow-hidden rounded-xl">
      <div className=" absolute top-0 left-0 bottom-0 right-0 h-full bg-gradient-to-b from-transparent from-0% to-dark/90 rounded-xl z-00" />
      <Image
        src={blog.image.filePath.replace("../public", "")}
        placeholder="blur"
        blurDataURL={blog.image.blurhashDataUrl}
        alt={blog.title}
        width={blog.image.width}
        height={blog.image.height}
        className="w-full h-full object-center object-cover rounded-xl group-hover:scale-105 transition-all ease duration-300"
        sizes="(max-width: 1180px) 100vw, 50vw"
      />
      <div className=" w-full absolute bottom-0 p-4 xs:p-6 sm:p-10 z-20">
        <Tag
          link={\`/categories/\${blog.tags[0]}\`}
          name={blog.tags[0]}
          className="px-6 text-xs sm:text-sm py-1 sm:py-2 !border"
        />
        <Link href={blog.url} className="mt-6">
          <h2 className="font-bold capitalize text-sm xs:text-base sm:text-xl md:text-2xl text-light mt-2 sm:mt-4">
            <span className="bg-gradient-to-r from-accent to-accent bg-[length:0px_6px] group-hover:bg-[length:100%_6px] bg-left-bottom bg-no-repeat transition-[background-size] duration-500">
              {blog.title}
            </span>
          </h2>
        </Link>
      </div>
    </div>
  );
};

export default BlogLayoutOne;`);
    document.getElementById("copy16").textContent = "✔️ copied";
    setTimeout(() => {
      document.getElementById("copy16").textContent = "";
    }, 2000);
  }}
>
  <div>📄 Code Copy</div>
</button>
<span id="copy16" style={{ marginLeft: "10px", color: "green" }}></span>

```javascript title="src/components/Blog/BlogLayoutTwo.js"
import { format } from "date-fns";
import Image from "next/image";
import Link from "next/link";
import React from "react";

const BlogLayoutTwo = ({ blog }) => {
  return (
    <div className="group grid grid-cols-12 gap-4 items-center text-dark dark:text-light">
      <Link
        href={blog.url}
        className="col-span-12 lg:col-span-4 rounded-xl overflow-hidden"
      >
        <Image
          src={blog.image.filePath.replace("../public", "")}
          placeholder="blur"
          blurDataURL={blog.image.blurhashDataUrl}
          alt={blog.title}
          width={blog.image.width}
          height={blog.image.height}
          className=" aspect-square w-full h-full object-cover object-center group-hover:scale-105 transition-all ease duration-300"
          sizes="(max-width: 640px) 100vw, (max-width: 1024px) 50vw, 33vw"
        />
      </Link>
      <div className="col-span-12 lg:col-span-8 w-full">
        <span className="inline-block w-full uppercase text-accent dark:text-accentDark font-semibold text-xs sm:text-sm">
          {blog.tags[0]}
        </span>
        <Link href={blog.url} className="inline-block my-1">
          <h2 className="font-semibold capitalize text-base sm:text-lg">
            <span className="bg-gradient-to-r from-accent/50 dark:from-accentDark/50 to-accent/50 dark:to-accentDark/50 bg-[length:0px_6px] group-hover:bg-[length:100%_6px] bg-left-bottom bg-no-repeat transition-[background-size] duration-500">
              {blog.title}
            </span>
          </h2>
        </Link>

        <div className="inline-block w-full capitalize text-dark/50 dark:text-light/50 font-semibold text-sm sm:text-base">
          {format(new Date(blog.publishedAt), "MMMM dd, yyyy")}
        </div>
      </div>
    </div>
  );
};

export default BlogLayoutTwo;
```

<button
  onClick={() => {
    navigator.clipboard.writeText(`
import { format } from "date-fns";
import Image from "next/image";
import Link from "next/link";
import React from "react";
const BlogLayoutTwo = ({ blog }) => {
  return (
    <div className="group grid grid-cols-12 gap-4 items-center text-dark dark:text-light">
      <Link
        href={blog.url}
        className="col-span-12 lg:col-span-4 rounded-xl overflow-hidden"
      >
        <Image
          src={blog.image.filePath.replace("../public", "")}
          placeholder="blur"
          blurDataURL={blog.image.blurhashDataUrl}
          alt={blog.title}
          width={blog.image.width}
          height={blog.image.height}
          className=" aspect-square w-full h-full object-cover object-center group-hover:scale-105 transition-all ease duration-300"
          sizes="(max-width: 640px) 100vw, (max-width: 1024px) 50vw, 33vw"
        />
      </Link>
      <div className="col-span-12 lg:col-span-8 w-full">
        <span className="inline-block w-full uppercase text-accent dark:text-accentDark font-semibold text-xs sm:text-sm">
          {blog.tags[0]}
        </span>
        <Link href={blog.url} className="inline-block my-1">
          <h2 className="font-semibold capitalize text-base sm:text-lg">
            <span className="bg-gradient-to-r from-accent/50 dark:from-accentDark/50 to-accent/50 dark:to-accentDark/50 bg-[length:0px_6px] group-hover:bg-[length:100%_6px] bg-left-bottom bg-no-repeat transition-[background-size] duration-500">
              {blog.title}
            </span>
          </h2>
        </Link>
        <div className="inline-block w-full capitalize text-dark/50 dark:text-light/50 font-semibold text-sm sm:text-base">
          {format(new Date(blog.publishedAt), "MMMM dd, yyyy")}
        </div>
      </div>
    </div>
  );
};

export default BlogLayoutTwo;`);
    document.getElementById("copy17").textContent = "✔️ copied";
    setTimeout(() => {
      document.getElementById("copy17").textContent = "";
    }, 2000);
  }}
>
  <div>📄 Code Copy</div>
</button>
<span id="copy17" style={{ marginLeft: "10px", color: "green" }}></span>

```javascript title="src/components/Blog/BlogLayoutThree.js"
import { format } from "date-fns";
import Image from "next/image";
import Link from "next/link";
import React from "react";

const BlogLayoutThree = ({ blog }) => {
  return (
    <div className="group flex flex-col items-center text-dark dark:text-light">
      <Link href={blog.url} className="rounded-xl overflow-hidden">
        <Image
          src={blog.image.filePath.replace("../public", "")}
          placeholder="blur"
          blurDataURL={blog.image.blurhashDataUrl}
          alt={blog.title}
          width={blog.image.width}
          height={blog.image.height}
          className=" aspect-[4/3] w-full h-full object-cover object-center group-hover:scale-105 transition-all ease duration-300"
          sizes="(max-width: 640px) 100vw, (max-width: 1024px) 50vw, 33vw"
        />
      </Link>
      <div className=" flex flex-col mt-4 w-full">
        <span className=" uppercase text-accent dark:text-accentDark font-semibold text-xs sm:text-sm">
          {blog.tags[0]}
        </span>
        <Link href={blog.url} className="inline-block my-1">
          <h2 className="font-semibold capitalize text-base sm:text-lg">
            <span className="bg-gradient-to-r from-accent/50 to-accent/50 dark:from-accentDark/50 dark:to-accentDark/50 bg-[length:0px_6px] group-hover:bg-[length:100%_6px] bg-left-bottom bg-no-repeat transition-[background-size] duration-500">
              {blog.title}
            </span>
          </h2>
        </Link>

        <div className="capitalize text-dark/50 dark:text-light/50 text-sm sm:text-base font-semibold">
          {format(new Date(blog.publishedAt), "MMMM dd, yyyy")}
        </div>
      </div>
    </div>
  );
};

export default BlogLayoutThree;
```

<button
  onClick={() => {
    navigator.clipboard.writeText(`
import { format } from "date-fns";
import Image from "next/image";
import Link from "next/link";
import React from "react";
const BlogLayoutThree = ({ blog }) => {
  return (
    <div className="group flex flex-col items-center text-dark dark:text-light">
      <Link href={blog.url} className="rounded-xl overflow-hidden">
        <Image
          src={blog.image.filePath.replace("../public", "")}
          placeholder="blur"
          blurDataURL={blog.image.blurhashDataUrl}
          alt={blog.title}
          width={blog.image.width}
          height={blog.image.height}
          className=" aspect-[4/3] w-full h-full object-cover object-center group-hover:scale-105 transition-all ease duration-300"
          sizes="(max-width: 640px) 100vw, (max-width: 1024px) 50vw, 33vw"
        />
      </Link>
      <div className=" flex flex-col mt-4 w-full">
        <span className=" uppercase text-accent dark:text-accentDark font-semibold text-xs sm:text-sm">
          {blog.tags[0]}
        </span>
        <Link href={blog.url} className="inline-block my-1">
          <h2 className="font-semibold capitalize text-base sm:text-lg">
            <span className="bg-gradient-to-r from-accent/50 to-accent/50 dark:from-accentDark/50 dark:to-accentDark/50 bg-[length:0px_6px] group-hover:bg-[length:100%_6px] bg-left-bottom bg-no-repeat transition-[background-size] duration-500">
              {blog.title}
            </span>
          </h2>
        </Link>
        <div className="capitalize text-dark/50 dark:text-light/50 text-sm sm:text-base font-semibold">
          {format(new Date(blog.publishedAt), "MMMM dd, yyyy")}
        </div>
      </div>
    </div>
  );
};

export default BlogLayoutThree;`);
    document.getElementById("copy18").textContent = "✔️ copied";
    setTimeout(() => {
      document.getElementById("copy18").textContent = "";
    }, 2000);
  }}
>
  <div>📄 Code Copy</div>
</button>
<span id="copy18" style={{ marginLeft: "10px", color: "green" }}></span>

```javascript title="src/components/About/AboutCoverSection.js"
import Image from "next/image";
import React from "react";
import profileCharater from "@/public/character.png";

const AboutCoverSection = () => {
  return (
    <section className="w-full md:h-[75vh] border-b-2 border-dark dark:border-light flex flex-col md:flex-row items-center justify-center text-dark dark:text-light">
      <div className="w-full md:w-1/2 h-full border-r-2 border-dark dark:border-light flex justify-center">
        <Image
          src={profileCharater}
          alt="Baekki"
          className="w-4/5 xs:w-3/4 md:w-full h-full object-contain object-center"
          priority
          sizes="(max-width: 768px) 100vw, (max-width: 1180px) 50vw, 50vw"
        />
      </div>

      <div className="w-full md:w-1/2 flex flex-col justify-center px-5 xs:p-10 pb-10 lg:px-16">
        <h2 className="font-bold capitalize text-4xl xs:text-5xl sxl:text-6xl text-center lg:text-left">
          Imagine Limitlessly, Strive Relentlessly, Surpass Expectations!
        </h2>
        <p className="font-medium capitalize mt-4">
          In the fields of blockchain and front-end development, I am dedicated
          to redefining user experiences with innovative solutions.
          Additionally, I employ modern web technologies to design intuitive and
          visually compelling interfaces, ensuring seamless interaction and
          navigation for users. If you&apos;re envisioning a project, let&apos;s
          collaborate to bring it to fruition.
        </p>
      </div>
    </section>
  );
};

export default AboutCoverSection;
```

<button
  onClick={() => {
    navigator.clipboard.writeText(`
import Image from "next/image";
import React from "react";
import profileCharater from "@/public/character.png";
const AboutCoverSection = () => {
  return (
    <section className="w-full md:h-[75vh] border-b-2 border-dark dark:border-light flex flex-col md:flex-row items-center justify-center text-dark dark:text-light">
      <div className="w-full md:w-1/2 h-full border-r-2 border-dark dark:border-light flex justify-center">
        <Image
          src={profileCharater}
          alt="Baekki"
          className="w-4/5 xs:w-3/4 md:w-full h-full object-contain object-center"
          priority
          sizes="(max-width: 768px) 100vw, (max-width: 1180px) 50vw, 50vw"
        />
      </div>
      <div className="w-full md:w-1/2 flex flex-col justify-center px-5 xs:p-10 pb-10 lg:px-16">
        <h2 className="font-bold capitalize text-4xl xs:text-5xl sxl:text-6xl text-center lg:text-left">
          Imagine Limitlessly, Strive Relentlessly, Surpass Expectations!
        </h2>
        <p className="font-medium capitalize mt-4">
          In the fields of blockchain and front-end development, I am dedicated
          to redefining user experiences with innovative solutions.
          Additionally, I employ modern web technologies to design intuitive and
          visually compelling interfaces, ensuring seamless interaction and
          navigation for users. If you&apos;re envisioning a project, let&apos;s
          collaborate to bring it to fruition.
        </p>
      </div>
    </section>
  );
};

export default AboutCoverSection;`);
    document.getElementById("copy19").textContent = "✔️ copied";
    setTimeout(() => {
      document.getElementById("copy19").textContent = "";
    }, 2000);
  }}
>
  <div>📄 Code Copy</div>
</button>
<span id="copy19" style={{ marginLeft: "10px", color: "green" }}></span>

이미지 최적화 후 console.log에 기록이 남는 것을 지우는 설정을 적용해봅시다.

<Image
  src="/blogs/nextjs-blog-contentlayer/12/seo7.png"
  width="718"
  height="404"
  alt="Image"
  sizes="100vw"
/>

위 이미지처럼 개발 중 적용했던 console 기록이 남습니다. 해당 부분의 console.log를 삭제해도 되지만, next.config.js에서 간단하게 콘솔 기록을 지우도록 적용해봅시다.

```javascript title="next.config.js"
/** @type {import('next').NextConfig} */
const { withContentlayer } = require("next-contentlayer");

const nextConfig = {
  compiler: {
    removeConsole: true,
  },
};

module.exports = withContentlayer({ ...nextConfig });
```

<button
  onClick={() => {
    navigator.clipboard.writeText(`
/** @type {import('next').NextConfig} */
const { withContentlayer } = require("next-contentlayer");
const nextConfig = {
  compiler: {
    removeConsole: true,
  },
};
module.exports = withContentlayer({ ...nextConfig });`);
    document.getElementById("copy20").textContent = "✔️ copied";
    setTimeout(() => {
      document.getElementById("copy20").textContent = "";
    }, 2000);
  }}
>
  <div>📄 Code Copy</div>
</button>
<span id="copy20" style={{ marginLeft: "10px", color: "green" }}></span>

compiler에 removeConsole을 적용하시면 됩니다.

⚠️ 아래 누락된 priority 속성을 추가해주세요.

```javascript title="src/app/blogs/[slug]/page.js"
<Image
  src={blog.image.filePath.replace("../public", "")}
  placeholder="blur"
  blurDataURL={blog.image.blurhashDataUrl}
  alt={blog.title}
  width={blog.image.width}
  height={blog.image.height}
  className=" aspect-square w-full h-full object-cover object-center"
  priority
  sizes="100vw"
/>
```

<button
  onClick={() => {
    navigator.clipboard.writeText(`
<Image
  src={blog.image.filePath.replace("../public", "")}
  placeholder="blur"
  blurDataURL={blog.image.blurhashDataUrl}
  alt={blog.title}
  width={blog.image.width}
  height={blog.image.height}
  className=" aspect-square w-full h-full object-cover object-center"
  priority
  sizes="100vw"
/>`);
    document.getElementById("copy21").textContent = "✔️ copied";
    setTimeout(() => {
      document.getElementById("copy21").textContent = "";
    }, 2000);
  }}
>
  <div>📄 Code Copy</div>
</button>
<span id="copy21" style={{ marginLeft: "10px", color: "green" }}></span>

## Google Structured json-id

JSON-LD(JSON for Linked Data)는 웹 페이지의 내용을 검색 엔진이 이해할 수 있도록 구조화된 데이터를 제공하는 포맷입니다.

<a
  href="https://nextjs.org/docs/app/building-your-application/optimizing/metadata#json-ld"
  target="_blank"
>
  next.js JSON-LD
</a>

<a
  href="https://developers.google.com/search/docs/appearance/structured-data/article"
  target="_blank"
>
  Google Structured data
</a>

위 두개의 공식문서에서 예제 코드를 가져와 작성해봅시다.

```javascript title="src/app/blogs/[slug]/page.js"
import { allBlogs } from ".contentlayer/generated";
import BlogDetails from "@/src/components/Blog/BlogDetails";
import RenderMdx from "@/src/components/Blog/RenderMdx";
import Tag from "@/src/components/Elements/Tag";
import siteMetadata from "@/src/utils/siteMetadata";
import { slug } from "github-slugger";
import Image from "next/image";

export async function generateStaticParams() {
  return allBlogs.map((blog) => ({ slug: blog._raw.flattenedPath }));
}

export async function generateMetadata({ params }) {
  const blog = allBlogs.find((blog) => blog._raw.flattenedPath === params.slug);

  if (!blog) {
    return;
  }

  const publishedAt = new Date(blog.publishedAt).toISOString();
  const modifiedAt = new Date(blog.updatedAt || blog.publishedAt).toISOString();

  let imageList = [siteMetadata.socialBanner];
  if (blog.image) {
    imageList =
      typeof blog.image.filePath === "string"
        ? [siteMetadata.siteUrl + blog.image.filePath.replace("../public", "")]
        : blog.image;
  }

  const ogImages = imageList.map((img) => {
    return [{ url: img.includes("http") ? img : siteMetadata.siteUrl + img }];
  });

  const authors = blog?.author ? blog.author : siteMetadata.author;

  return {
    title: blog.title,
    description: blog.description,
    openGraph: {
      title: blog.title,
      description: blog.description,
      url: siteMetadata.siteUrl + blog.url,
      siteName: siteMetadata.title,
      locale: "ko-kr",
      type: "article",
      publishedTime: publishedAt,
      modifiedTime: modifiedAt,
      images: ogImages,
      author: authors.length > 0 ? authors : [siteMetadata.author],
    },
    twitter: {
      card: "summary_large_image",
      title: blog.title,
      description: blog.description,
      images: ogImages,
    },
  };
}

export default function BlogPage({ params }) {
  const blog = allBlogs.find((blog) => blog._raw.flattenedPath === params.slug);

  let imageList = [siteMetadata.socialBanner];
  if (blog.image) {
    imageList =
      typeof blog.image.filePath === "string"
        ? [siteMetadata.siteUrl + blog.image.filePath.replace("../public", "")]
        : blog.image;
  }

  const jsonLd = {
    "@context": "https://schema.org",
    "@type": "NewsArticle",
    headline: blog.title,
    description: blog.description,
    image: imageList,
    datePublished: new Date(blog.publishedAt).toISOString(),
    dateModified: new Date(blog.updatedAt || blog.publishedAt).toISOString(),
    author: [
      {
        "@type": "Person",
        name: blog?.author ? blog.author : siteMetadata.author,
        url: siteMetadata.linkedin,
      },
    ],
  };

  return (
    <>
      <script
        type="application/ld+json"
        dangerouslySetInnerHTML={{ __html: JSON.stringify(jsonLd) }}
      />
      <article>
        <div className="mb-8 text-center relative w-full h-[70vh] bg-dark">
          <div className="w-full z-10 flex flex-col items-center justify-center absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2">
            <Tag
              name={blog.tags[0]}
              link={`/categories/${slug(blog.tags[0])}`}
              className="px-6 text-sm py-2"
            />
            <h1 className="inline-block mt-6 font-semibold capitalize text-light text-2xl md:text-3xl lg:text-5xl leading-normal relative w-5/6">
              {blog.title}
            </h1>
          </div>
          <div className="absolute top-0 left-0 right-0 bottom-0 h-full bg-dark/60 dark:bg-dark/40" />
          <Image
            src={blog.image.filePath.replace("../public", "")}
            placeholder="blur"
            blurDataURL={blog.image.blurhashDataUrl}
            alt={blog.title}
            width={blog.image.width}
            height={blog.image.height}
            className=" aspect-square w-full h-full object-cover object-center"
            priority
            sizes="100vw"
          />
        </div>
        <BlogDetails blog={blog} slug={params.slug} />
        <div className="grid grid-cols-12 gap-y-8 lg:gap-8 sxl:gap-16 mt-8 px-5 md:px-10">
          <div className="col-span-12 lg:col-span-4">
            <details
              className="border border-dark dark:border-light text-dark dark:text-light rounded-lg p-4 sticky top-6 max-h-[80vh] overflow-hidden overflow-y-auto"
              open
            >
              <summary className="text-lg font-semibold capitalize cursor-pointer">
                Table Of Contents
              </summary>
              <ul className="mt-4 font-in">
                {blog.toc.map((heading) => {
                  return (
                    <li key={`#${heading.slug}`} className="py-1">
                      <a
                        href={`#${heading.slug}`}
                        data-level={heading.level}
                        className="data-[level=two]:pl-0 data-[level=two]:pt-2
                    data-[level=two]:border-b border-dark/40 data-[level=three]:pl-3 sm:data-[level=three]:pl-6
                    flex items-center"
                      >
                        {heading.level === "three" ? (
                          <span className="flex w-1 h-1 rounded-full bg-dark mr-2 ">
                            &nbsp;
                          </span>
                        ) : null}
                        <span className="hover:underline">{heading.text}</span>
                      </a>
                    </li>
                  );
                })}
              </ul>
            </details>
          </div>
          <RenderMdx blog={blog} />
        </div>
      </article>
    </>
  );
}
```

<button
  onClick={() => {
    navigator.clipboard.writeText(`
import { allBlogs } from ".contentlayer/generated";
import BlogDetails from "@/src/components/Blog/BlogDetails";
import RenderMdx from "@/src/components/Blog/RenderMdx";
import Tag from "@/src/components/Elements/Tag";
import siteMetadata from "@/src/utils/siteMetadata";
import { slug } from "github-slugger";
import Image from "next/image";

export async function generateStaticParams() {
  return allBlogs.map((blog) => ({ slug: blog._raw.flattenedPath }));
}

export async function generateMetadata({ params }) {
  const blog = allBlogs.find((blog) => blog._raw.flattenedPath === params.slug);
  if (!blog) {
    return;
  }
  const publishedAt = new Date(blog.publishedAt).toISOString();
  const modifiedAt = new Date(blog.updatedAt || blog.publishedAt).toISOString();
  let imageList = [siteMetadata.socialBanner];
  if (blog.image) {
    imageList =
      typeof blog.image.filePath === "string"
        ? [siteMetadata.siteUrl + blog.image.filePath.replace("../public", "")]
        : blog.image;
  }
  const ogImages = imageList.map((img) => {
    return [{ url: img.includes("http") ? img : siteMetadata.siteUrl + img }];
  });
  const authors = blog?.author ? blog.author : siteMetadata.author;
  return {
    title: blog.title,
    description: blog.description,
    openGraph: {
      title: blog.title,
      description: blog.description,
      url: siteMetadata.siteUrl + blog.url,
      siteName: siteMetadata.title,
      locale: "ko-kr",
      type: "article",
      publishedTime: publishedAt,
      modifiedTime: modifiedAt,
      images: ogImages,
      author: authors.length > 0 ? authors : [siteMetadata.author],
    },
    twitter: {
      card: "summary_large_image",
      title: blog.title,
      description: blog.description,
      images: ogImages,
    },
  };
}

export default function BlogPage({ params }) {
  const blog = allBlogs.find((blog) => blog._raw.flattenedPath === params.slug);
  let imageList = [siteMetadata.socialBanner];
  if (blog.image) {
    imageList =
      typeof blog.image.filePath === "string"
        ? [siteMetadata.siteUrl + blog.image.filePath.replace("../public", "")]
        : blog.image;
  }
  const jsonLd = {
    "@context": "https://schema.org",
    "@type": "NewsArticle",
    headline: blog.title,
    description: blog.description,
    image: imageList,
    datePublished: new Date(blog.publishedAt).toISOString(),
    dateModified: new Date(blog.updatedAt || blog.publishedAt).toISOString(),
    author: [
      {
        "@type": "Person",
        name: blog?.author ? blog.author : siteMetadata.author,
        url: siteMetadata.linkedin,
      },
    ],
  };
  return (
    <>
      <script
        type="application/ld+json"
        dangerouslySetInnerHTML={{ __html: JSON.stringify(jsonLd) }}
      />
      <article>
        <div className="mb-8 text-center relative w-full h-[70vh] bg-dark">
          <div className="w-full z-10 flex flex-col items-center justify-center absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2">
            <Tag
              name={blog.tags[0]}
              link={\`/categories/\${slug(blog.tags[0])}\`}
              className="px-6 text-sm py-2"
            />
            <h1 className="inline-block mt-6 font-semibold capitalize text-light text-2xl md:text-3xl lg:text-5xl leading-normal relative w-5/6">
              {blog.title}
            </h1>
          </div>
          <div className="absolute top-0 left-0 right-0 bottom-0 h-full bg-dark/60 dark:bg-dark/40" />
          <Image
            src={blog.image.filePath.replace("../public", "")}
            placeholder="blur"
            blurDataURL={blog.image.blurhashDataUrl}
            alt={blog.title}
            width={blog.image.width}
            height={blog.image.height}
            className=" aspect-square w-full h-full object-cover object-center"
            priority
            sizes="100vw"
          />
        </div>
        <BlogDetails blog={blog} slug={params.slug} />
        <div className="grid grid-cols-12 gap-y-8 lg:gap-8 sxl:gap-16 mt-8 px-5 md:px-10">
          <div className="col-span-12 lg:col-span-4">
            <details
              className="border border-dark dark:border-light text-dark dark:text-light rounded-lg p-4 sticky top-6 max-h-[80vh] overflow-hidden overflow-y-auto"
              open
            >
              <summary className="text-lg font-semibold capitalize cursor-pointer">
                Table Of Contents
              </summary>
              <ul className="mt-4 font-in">
                {blog.toc.map((heading) => {
                  return (
                    <li key={\`#\${heading.slug}\`} className="py-1">
                      <a
                        href={\`#\${heading.slug}\`}
                        data-level={heading.level}
                        className="data-[level=two]:pl-0 data-[level=two]:pt-2
                    data-[level=two]:border-b border-dark/40 data-[level=three]:pl-3 sm:data-[level=three]:pl-6
                    flex items-center"
                      >
                        {heading.level === "three" ? (
                          <span className="flex w-1 h-1 rounded-full bg-dark mr-2 ">
                            &nbsp;
                          </span>
                        ) : null}
                        <span className="hover:underline">{heading.text}</span>
                      </a>
                    </li>
                  );
                })}
              </ul>
            </details>
          </div>
          <RenderMdx blog={blog} />
        </div>
      </article>
    </>
  );
}`);
    document.getElementById("copy22").textContent = "✔️ copied";
    setTimeout(() => {
      document.getElementById("copy22").textContent = "";
    }, 2000);
  }}
>
  <div>📄 Code Copy</div>
</button>
<span id="copy22" style={{ marginLeft: "10px", color: "green" }}></span>

<Image
  src="/blogs/nextjs-blog-contentlayer/12/seo8.png"
  width="718"
  height="404"
  alt="Image"
  sizes="100vw"
/>

위 처럼 적용된 것을 볼 수 있습니다.

## 다음 포스팅 보러가기

<a href="https://baekki-dev-blog.vercel.app/blogs/Next.js-blog-with-contentlayer-13">
  13. Dark mode
</a>
